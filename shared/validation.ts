import { z } from 'zod';
export const checkoutSchema=z.object({idempotencyKey:z.string().uuid(),locale:z.enum(['ar','he','en']),firstName:z.string().trim().min(2).max(60),lastName:z.string().trim().min(2).max(60),email:z.string().trim().email().max(254),phone:z.string().trim().regex(/^(?:\+972|972|0)[\d -]{8,14}$/),city:z.string().trim().min(2).max(80),street:z.string().trim().min(4).max(180),postalCode:z.string().regex(/^(?:\d{5}|\d{7})?$/),country:z.literal('IL'),notes:z.string().max(1000),giftNote:z.string().max(500),marketingConsent:z.boolean(),whatsappUpdates:z.boolean(),zoneId:z.string().min(1),paymentMethod:z.enum(['bank_transfer','paypal','card','bit','paybox']),discountCode:z.string().max(50),lines:z.array(z.object({productId:z.string().min(1),variantId:z.string().min(1),quantity:z.number().int().min(1).max(10)})).min(1).max(50),turnstileToken:z.string().max(4096).optional()}).strict();
